# LearningIsFun 测试题修复 - 2026-04-18 22:12

## 问题

1. **新词与待复习词混淆**：688个词全部被标记为"待复习"，用户没有新词可学
2. **测试题输入框不可见**：填空题在微信浏览器中输入框无法显示

## 修复

### 1. 新词与待复习词区分 (commit a67a511)
- 修改 `getDueWords()` 只返回已学过（有 `lastReview`）且到期待复习的词
- 新词（`lastReview === null`）不再自动进入复习队列

### 2. 测试题选项按钮背景色 (commit cf0f656)
- 选项按钮加 `btn-secondary` 类，确保有背景色可见

### 3. 去掉填空题 (commit b6923a2)
- 测试全部改为4选1选择题
- 显示英文单词，选中文释义
- 避免微信浏览器输入框兼容问题

## 结果

- 新词：688个（`lastReview === null`）
- 待复习：0个（因为没有已学过的词到期）
- 测试：10道4选1选择题
